# Conflict Resolution

Use the source precedence in `AGENTS.md`. For parallel work, resolve integration through Git commits/PRs rather than concurrent edits in one worktree. When two changes conflict semantically, prefer the accepted architecture and current issue requirements; do not silently choose. Escalate unresolved material conflicts to the human owner.
